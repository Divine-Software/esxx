#!/usr/bin/env esxx-js

esxx.debug.println("Direct JS, with shebang");

function main(args) {
  esxx.debug.println("main() called: " + args);

  for each (a in args) {
      esxx.debug.println("Argument: " + a);
  }

  return 202;
}
